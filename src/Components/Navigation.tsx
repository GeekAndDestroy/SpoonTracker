

type NavigationProps = {    
        isLoggedIn: boolean,
        logUserOut: () => void,
}

export default function Navigation({  }: NavigationProps) {
    return (
        <div>Navigation</div>
    )
}